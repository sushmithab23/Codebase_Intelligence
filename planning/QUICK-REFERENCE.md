# CIE — Quick Reference Card
## Print this. Stick it on the wall. Read it when lost.

---

## The only thing that matters

**3 moments. 3 minutes. In this order. Every time.**

1. URL in → map appears → judge goes "oh wow"
2. Question typed → map highlights → judge goes "how did it know?"
3. Module clicked → code generated → judge goes "I want this"

---

## Person A owns (Frontend)
- React app at `localhost:3000`
- D3 map component
- Impact highlight overlay
- Generate panel with syntax highlighting
- Loading states and error states

**Your success metric:** The map looks stunning and never crashes.

---

## Person B owns (Backend)
- Node server at `localhost:4000`
- `/api/analyse` → module map JSON
- `/api/impact` → affected module IDs
- `/api/generate` → tests + docs code

**Your success metric:** All 3 endpoints return correct JSON in under 10 seconds.

---

## Person C owns (Bob + Demo)
- `.bob/rules.md` tuning
- Bob prompt testing and refinement
- Demo script (memorised)
- Pitch narrative
- Backup screen recording
- Submission form

**Your success metric:** Demo runs cleanly 5 times in a row before presentation.

---

## If something breaks during demo

| Break | Fix |
|---|---|
| Map doesn't render | Switch to backup screenshot |
| Bob times out | Show pre-cached response |
| API errors | Switch to recorded video |
| D3 graph looks ugly | Zoom out and keep talking confidently |

**Never apologise. Never stop. Keep narrating.**

---

## Judging criteria reminder

| Points | What they want |
|---|---|
| Completeness (5) | Does it actually work end to end? |
| Creativity (5) | Is the impact-on-map idea genuinely new? |
| Usability (5) | Can anyone use it in 30 seconds? |
| Effectiveness (5) | Can you measure the time saved? |

**Say these numbers in your pitch:**
- "Onboarding: days → minutes"
- "Tests: hours → seconds"
- "Impact analysis: grep and prayer → one question"

---

## Demo repo
**URL:** https://github.com/expressjs/express  
**Impact demo function:** `router.handle`  
**Module to generate tests for:** middleware layer

---

## Submission deadline
Check the hackathon page. Submit 30 minutes early. Never cut it close.
